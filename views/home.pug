extends layouts/main
include mixins/videoBlock

block content
    div.videos
        each items in videos
            +videoBlock({
                title: items.title,
                views: items.views,
                videoFile: items.videoFile
            })